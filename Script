loadstring(game:HttpGet("https://raw.githubusercontent.com/eoyoustme/Hardcore/refs/heads/main/Stamina"))()
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local TweenService = game:GetService("TweenService")
local Debris = game:GetService("Debris")
 
local player = Players.LocalPlayer
local playerGui = player:FindFirstChild("PlayerGui")
local mainUI = playerGui and playerGui:FindFirstChild("MainUI")
local initiator = mainUI and mainUI:FindFirstChild("Initiator")
 
if initiator then
    local mainGame = initiator:FindFirstChild("Main_Game")
    if mainGame and mainGame:IsA("ModuleScript") then
        local mainGameModule = require(mainGame)
        if type(mainGameModule.caption) == "function" then
            mainGameModule.caption("negative mode By Rebound", true)
            wait(4)
            mainGameModule.caption("Credits to T_10006 for A-60 rbxm, and Credit to me for the models", true)
            wait(3)
            mainGameModule.caption("you will have fun (you won’t)", true)
        end
    end
end

-- Blizzard Entity 
coroutine.wrap(function()
    while true do
        wait(math.random(225,350))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()

---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/eoyoustme/Endless-Doors/refs/heads/main/Endless%20Doors%20Spawner"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "Blizzard",
		Asset = "https://github.com/eliazbp92-collab/WHAT-IS-A-SOILD-OCTO-ROTRAY-PHONE-/raw/main/Place_10959918411_Model_BackdoorRush_1786310277.rbxm",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = true,
			Duration = 1
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {5, 15, 0.1, 1}
	},
	Movement = {
		Speed = 200,
		Delay = 10, 
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush",
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 33,
		Amount = 100
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding",
		Hints = {"You die to Blizzard.", "Blizzard is a clear blue verison blitz.", "If you hear low pitch of Rush sound, Hide", "Blizzard sometime be tricky for being fast."},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local cue2 = Instance.new("Sound")
		cue2.Parent = game.Workspace
		cue2.Name = "Spawn"
		cue2.SoundId = "rbxassetid://74885373257467"
		cue2.Volume = 10
		cue2.TimePosition = 0
		cue2.PlaybackSpeed = 0.82
		cue2:Play()

		local pitch = Instance.new('PitchShiftSoundEffect')
		pitch.Octave = 0.605
		pitch.Parent = cue2

		local dis = Instance.new("DistortionSoundEffect")
		dis.Parent = cue2

		local eq = Instance.new("EqualizerSoundEffect")
		eq.HighGain = 10
		eq.LowGain = 3.7
		eq.MidGain = 10
		eq.Parent = cue2

		local playerGui = game.Players.LocalPlayer:WaitForChild("PlayerGui")
		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "FlashOverlay"
		screenGui.IgnoreGuiInset = true -- Fills the whole screen
		screenGui.Parent = playerGui

		-- Create the Frame
		local frame = Instance.new("Frame")
		frame.Size = UDim2.new(1, 0, 1, 0)
		frame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		frame.BackgroundTransparency = 0 -- Start invisible
		frame.BorderSizePixel = 0
		frame.Parent = screenGui



		local Smiler = Instance.new("ImageLabel")
		Smiler.Image = "rbxassetid://72986967979824"
		Smiler.Size = UDim2.new(0, 60, 0, 60)
		Smiler.Position = UDim2.new(0.5, 0,0.5, 0)
		Smiler.ImageColor3 = Color3.fromRGB(255, 35, 35)
		Smiler.AnchorPoint = Vector2.new(0.5, 0.5)
		Smiler.Parent = screenGui
		Smiler.BackgroundTransparency = 1
		Smiler.ResampleMode = Enum.ResamplerMode.Pixelated
		Smiler.ImageTransparency = 0
		Smiler.ZIndex = 1000
		Smiler.Rotation = 0


		local a = task.spawn(function()
			while true do
				frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
				wait(0)
				frame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
				wait(0)
			end
		end)

		local rng = Random.new()
		local origin = Smiler.Position

		local b = task.spawn(function()
			while true do
				Smiler.ImageColor3 = Color3.fromRGB(math.random(245, 255), math.random(245, 255), math.random(245, 255))
				Smiler.Rotation = math.random(-15, 15)
				Smiler.ImageTransparency = math.random(0, 50) / 100
				Smiler.Position = origin + UDim2.new(0, math.random(-60, 60), 0, math.random(-60, 60))
				wait(0.002380952380952381)
			end
		end)

		local TweenService = game:GetService("TweenService")
		local rng = Random.new() 
		local tweenInfo = TweenInfo.new(0.7, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

		game.TweenService:Create(Smiler, tweenInfo, {

			Size = UDim2.new(0,3000,0,2500)
		}):Play()

		wait(0.7)
		screenGui:Destroy()		
		cue2:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

---====== Run entity ======---

entity:Run(true)
 
        end)
    end
end)()

-- Silence Entity
coroutine.wrap(function()
    while true do
        wait(math.random(290,320))
        pcall(function()
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
           ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/eoyoustme/Endless-Doors/refs/heads/main/Endless%20Doors%20Spawner"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "Silence",
		Asset = "https://github.com/eliazbp92-collab/WHAT-IS-A-SOILD-OCTO-ROTRAY-PHONE-/raw/main/Place_10959918411_Model_Silence_1786304856.rbxm",
		HeightOffset = -0.5
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {5, 15, 0.1, 1}
	},
	Movement = {
		Speed = 20,
		Delay = 5, 
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush",
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 33,
		Amount = 100
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding",
		Hints = {"You die to Silence", You can hear his sound when spawn", "Hide when hear his sound", "Silence is may slow and tricky"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local cue2 = Instance.new("Sound")
		cue2.Parent = game.Workspace
		cue2.Name = "Spawn"
		cue2.SoundId = "rbxassetid://74885373257467"
		cue2.Volume = 10
		cue2.TimePosition = 0
		cue2.PlaybackSpeed = 0.82
		cue2:Play()

		local pitch = Instance.new('PitchShiftSoundEffect')
		pitch.Octave = 0.605
		pitch.Parent = cue2

		local dis = Instance.new("DistortionSoundEffect")
		dis.Parent = cue2

		local eq = Instance.new("EqualizerSoundEffect")
		eq.HighGain = 10
		eq.LowGain = 3.7
		eq.MidGain = 10
		eq.Parent = cue2

		local playerGui = game.Players.LocalPlayer:WaitForChild("PlayerGui")
		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "FlashOverlay"
		screenGui.IgnoreGuiInset = true -- Fills the whole screen
		screenGui.Parent = playerGui

		-- Create the Frame
		local frame = Instance.new("Frame")
		frame.Size = UDim2.new(1, 0, 1, 0)
		frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		frame.BackgroundTransparency = 0 -- Start invisible
		frame.BorderSizePixel = 0
		frame.Parent = screenGui



		local Smiler = Instance.new("ImageLabel")
		Smiler.Image = "rbxassetid://70668417233546"
		Smiler.Size = UDim2.new(0, 60, 0, 60)
		Smiler.Position = UDim2.new(0.5, 0,0.5, 0)
		Smiler.ImageColor3 = Color3.fromRGB(255, 35, 35)
		Smiler.AnchorPoint = Vector2.new(0.5, 0.5)
		Smiler.Parent = screenGui
		Smiler.BackgroundTransparency = 1
		Smiler.ResampleMode = Enum.ResamplerMode.Pixelated
		Smiler.ImageTransparency = 0
		Smiler.ZIndex = 1000
		Smiler.Rotation = 0


		local a = task.spawn(function()
			while true do
				frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
				wait(0)
				frame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
				wait(0)
			end
		end)

		local rng = Random.new()
		local origin = Smiler.Position

		local b = task.spawn(function()
			while true do
				Smiler.ImageColor3 = Color3.fromRGB(math.random(245, 255), math.random(245, 255), math.random(245, 255))
				Smiler.Rotation = math.random(-15, 15)
				Smiler.ImageTransparency = math.random(0, 50) / 100
				Smiler.Position = origin + UDim2.new(0, math.random(-60, 60), 0, math.random(-60, 60))
				wait(0.002380952380952381)
			end
		end)

		local TweenService = game:GetService("TweenService")
		local rng = Random.new() 
		local tweenInfo = TweenInfo.new(0.7, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

		game.TweenService:Create(Smiler, tweenInfo, {

			Size = UDim2.new(0,3000,0,2500)
		}):Play()

		wait(0.7)
		screenGui:Destroy()		
		cue2:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

---====== Run entity ======---

entity:Run(true)
 
        end)
    end
end)()

-- negivate Rush and negivate Ambush Entity
coroutine.wrap(function()
    while true do
        wait(math.random(1,2))
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/eliazbp92-collab/WHAT-IS-A-SOILD-OCTO-ROTRAY-PHONE-/refs/heads/main/Negative%20Ambush%20and%20negative%20rush"))()
        end)
    end
end)()

-- A-60 Entity
coroutine.wrap(function()
    while true do
        wait(math.random(200,210))
        pcall(function()

game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
           ---====== Load spawner ======---

local spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/V2/Source.lua"))()

---====== Create entity ======---

local entity = spawner.Create({
	Entity = {
		Name = "A-60",
		Asset = "https://github.com/eliazbp92-collab/Dark-passage-mode/raw/main/A-60.rbxm",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 150,
		Delay = 4,
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 23500
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Curious", -- "Curious"
		Hints = {"You died to A-60", "Run and Hide!"},
		Cause = "A-60"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
    print("Entity has spawned")

-- Âm thanh báo hiệu
local cue1 = Instance.new("Sound")
cue1.Parent = game.Workspace
cue1.Name = "Scream"
cue1.SoundId = "https://github.com/reboundplayz/Doors-Fixed-Modes/raw/refs/heads/main/X-60Spawn.mp3.mpeg"
 
local distort = Instance.new("DistortionSoundEffect", cue1)
distort.Level = 0
local distort2 = Instance.new("DistortionSoundEffect", cue1)
distort2.Level = 0
 
local pitch = Instance.new("PitchShiftSoundEffect", cue1)
pitch.Octave = 0.7
local pitch2 = Instance.new("PitchShiftSoundEffect", cue1)
pitch2.Octave = 0.7
local pitch3 = Instance.new("PitchShiftSoundEffect", cue1)
pitch3.Octave = 0.7
 
cue1.Volume = 5
cue1:Play()
end)

entity:SetCallback("OnStartMoving", function()
    print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room, firstTime)
    if firstTime == true then
        print("Entity has entered room: ".. room.Name.. " for the first time")
    else
        print("Entity has entered room: ".. room.Name.. " again")
    end
end)

entity:SetCallback("OnLookAt", function(lineOfSight)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound)
    if startOfRebound == true then
        print("Entity has started rebounding")
	else
        print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
    print("Entity is despawning")
wait(5)
end)

entity:SetCallback("OnDespawned", function()
    print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth)
	if newHealth == 0 then
		print("Entity has killed the player")
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run()
 
        end)
    end
end)()

-- Greed Entity
coroutine.wrap(function()
    while true do
        wait(math.random(300,450))
        pcall(function()

game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
           ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/eoyoustme/Endless-Doors/refs/heads/main/Endless%20Doors%20Spawner"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "Greed",
		Asset = "https://github.com/eliazbp92-collab/WHAT-IS-A-SOILD-OCTO-ROTRAY-PHONE-/raw/main/Place_10959918411_Model_Greed_1786316423.rbxm",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = true,
			Duration = 1
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {5, 15, 0.1, 1}
	},
	Movement = {
		Speed = 200,
		Delay = 10, 
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush",
		Min = 7,
		Max = 10,
		Delay = 0.5
	},
	Damage = {
		Enabled = true,
		Range = 33,
		Amount = 100
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding",
		Hints = {"You die to...", "Uhmm Matcher", "Hide when hear his sound", "Goodbye"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
-- Âm thanh báo hiệu
local cue1 = Instance.new("Sound")
cue1.Parent = game.Workspace
cue1.Name = "Scream"
cue1.SoundId = "rbxassetid://715673747"

local distort = Instance.new("DistortionSoundEffect", cue1)
distort.Level = 0
local distort2 = Instance.new("DistortionSoundEffect", cue1)
distort2.Level = 0

local pitch = Instance.new("PitchShiftSoundEffect", cue1)
pitch.Octave = 1
local pitch2 = Instance.new("PitchShiftSoundEffect", cue1)
pitch2.Octave = 1
local pitch3 = Instance.new("PitchShiftSoundEffect", cue1)
pitch3.Octave = 1

cue1.Volume = 5000000000000000
cue1:Play()
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local cue2 = Instance.new("Sound")
		cue2.Parent = game.Workspace
		cue2.Name = "Spawn"
		cue2.SoundId = "rbxassetid://74885373257467"
		cue2.Volume = 10
		cue2.TimePosition = 0
		cue2.PlaybackSpeed = 0.82
		cue2:Play()

		local pitch = Instance.new('PitchShiftSoundEffect')
		pitch.Octave = 0.605
		pitch.Parent = cue2

		local dis = Instance.new("DistortionSoundEffect")
		dis.Parent = cue2

		local eq = Instance.new("EqualizerSoundEffect")
		eq.HighGain = 10
		eq.LowGain = 3.7
		eq.MidGain = 10
		eq.Parent = cue2

		local playerGui = game.Players.LocalPlayer:WaitForChild("PlayerGui")
		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "FlashOverlay"
		screenGui.IgnoreGuiInset = true -- Fills the whole screen
		screenGui.Parent = playerGui

		-- Create the Frame
		local frame = Instance.new("Frame")
		frame.Size = UDim2.new(1, 0, 1, 0)
		frame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		frame.BackgroundTransparency = 0 -- Start invisible
		frame.BorderSizePixel = 0
		frame.Parent = screenGui



		local Smiler = Instance.new("ImageLabel")
		Smiler.Image = "rbxassetid://72986967979824"
		Smiler.Size = UDim2.new(0, 60, 0, 60)
		Smiler.Position = UDim2.new(0.5, 0,0.5, 0)
		Smiler.ImageColor3 = Color3.fromRGB(255, 35, 35)
		Smiler.AnchorPoint = Vector2.new(0.5, 0.5)
		Smiler.Parent = screenGui
		Smiler.BackgroundTransparency = 1
		Smiler.ResampleMode = Enum.ResamplerMode.Pixelated
		Smiler.ImageTransparency = 0
		Smiler.ZIndex = 1000
		Smiler.Rotation = 0


		local a = task.spawn(function()
			while true do
				frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
				wait(0)
				frame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
				wait(0)
			end
		end)

		local rng = Random.new()
		local origin = Smiler.Position

		local b = task.spawn(function()
			while true do
				Smiler.ImageColor3 = Color3.fromRGB(math.random(245, 255), math.random(245, 255), math.random(245, 255))
				Smiler.Rotation = math.random(-15, 15)
				Smiler.ImageTransparency = math.random(0, 50) / 100
				Smiler.Position = origin + UDim2.new(0, math.random(-60, 60), 0, math.random(-60, 60))
				wait(0.002380952380952381)
			end
		end)

		local TweenService = game:GetService("TweenService")
		local rng = Random.new() 
		local tweenInfo = TweenInfo.new(0.7, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

		game.TweenService:Create(Smiler, tweenInfo, {

			Size = UDim2.new(0,3000,0,2500)
		}):Play()

		wait(0.7)
		screenGui:Destroy()		
		cue2:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

---====== Run entity ======---

entity:Run(true)

        end)
    end
end)()

-- Hunger Entity
coroutine.wrap(function()
    while true do
        wait(math.random(290,350))
        pcall(function()

game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
           ---====== Load spawner ======---

local spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/V2/Source.lua"))()

---====== Create entity ======---

local entity = spawner.Create({
	Entity = {
		Name = "A-60",
		Asset = "https://github.com/eliazbp92-collab/Dark-passage-mode/raw/main/A-60.rbxm",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 150,
		Delay = 4,
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 23500
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Curious", -- "Curious"
		Hints = {"You died to A-60", "Run and Hide!"},
		Cause = "A-60"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
    print("Entity has spawned")

-- Âm thanh báo hiệu
local cue1 = Instance.new("Sound")
cue1.Parent = game.Workspace
cue1.Name = "Scream"
cue1.SoundId = "https://github.com/reboundplayz/Doors-Fixed-Modes/raw/refs/heads/main/X-60Spawn.mp3.mpeg"
 
local distort = Instance.new("DistortionSoundEffect", cue1)
distort.Level = 0
local distort2 = Instance.new("DistortionSoundEffect", cue1)
distort2.Level = 0
 
local pitch = Instance.new("PitchShiftSoundEffect", cue1)
pitch.Octave = 0.7
local pitch2 = Instance.new("PitchShiftSoundEffect", cue1)
pitch2.Octave = 0.7
local pitch3 = Instance.new("PitchShiftSoundEffect", cue1)
pitch3.Octave = 0.7
 
cue1.Volume = 5
cue1:Play()
end)

entity:SetCallback("OnStartMoving", function()
    print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room, firstTime)
    if firstTime == true then
        print("Entity has entered room: ".. room.Name.. " for the first time")
    else
        print("Entity has entered room: ".. room.Name.. " again")
    end
end)

entity:SetCallback("OnLookAt", function(lineOfSight)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound)
    if startOfRebound == true then
        print("Entity has started rebounding")
	else
        print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
    print("Entity is despawning")
wait(5)
end)

entity:SetCallback("OnDespawned", function()
    print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth)
	if newHealth == 0 then
		print("Entity has killed the player")
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run()
 
        end)
    end
end)()
